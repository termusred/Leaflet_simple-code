/** @format */

const cities = L.layerGroup([test]);

const parks = L.layerGroup([crownHill, rubyHill]);

const shopsContainer = L.layerGroup([shops , shops2]);

const schoolsCon = L.layerGroup([maktablar, maktablar2, maktablar3]);

const TeatorCon = L.layerGroup([kinoteator, kinoteator2]);

const DamnCon = L.layerGroup([Damn1, Damn2]);